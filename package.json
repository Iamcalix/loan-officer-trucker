{
  "name": "officer-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Live GPS map + route history for field officers (Wanway/IoP Open API).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  }
}
